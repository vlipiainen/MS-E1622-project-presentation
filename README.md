# Xiphera LaTeX document format

## Easiest way to install:

```bash
cd ~
mkdir texmf
mkdir texmf/tex
mkdir texmf/tex/latex
cd texmf/tex/latex
git clone git@192.168.1.200:xiphera/document/xipheradocument.git
sudo mktexlsr
```
